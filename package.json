{
  "name": "portfolio-monorepo",
  "private": true,
  "version": "1.0.0",
  "workspaces": [
    "backend",
    "admin",
    "desktop",
    "frontend"
  ],
  "scripts": {
    "dev:backend": "cd backend && npm run dev",
    "dev:admin": "cd admin && npm run dev",
    "dev:web": "cd frontend && npm run dev",
    "dev:electron": "cd desktop && npm run start",
    "dev:all": "concurrently -k -p \"[{name}]\" -n \"BACKEND,ADMIN,WEB\" -c \"blue.bold,magenta.bold,green.bold\" \"npm run dev:backend\" \"npm run dev:admin\" \"npm run dev:web\"",
    "build:admin": "cd admin && npm run build",
    "build:web": "cd frontend && npm run build",
    "build:all": "cd admin && npm run build && cd ../frontend && npm run build",
    "test:db": "cd backend && npm run test:db",
    "seed:atlas": "cd backend && npm run seed:atlas",
    "pack:electron": "cd admin && npm run build && cd ../desktop && npm run pack",
    "dist:electron": "cd admin && npm run build && cd ../desktop && npm run dist"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
